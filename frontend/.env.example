# URL of the FastAPI backend. Used at build time for the /api rewrite — redeploy after changing it.
BACKEND_URL=https://guessings-api.onrender.com
# Same value as PROXY_SECRET on the backend.
PROXY_SECRET=
